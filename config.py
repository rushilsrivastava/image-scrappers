hash = False     # a boolean specifying whether or not the tool will change the download file's name to a sha256 calculated hash
unique = True    # a boolean specifying if the downloaded files should be placed in their own unique folder, or the Google root.
imagepath = "./"   # this is changed at runtime.